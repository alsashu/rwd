import { TestBed } from "@angular/core/testing";

import { WikiFactoryService } from "./wiki-factory.service";

describe("WikiFactoryService", () => {
  // beforeEach(() => TestBed.configureTestingModule({}));
  // it('should be created', () => {
  //   const service: WikiFactoryService = TestBed.get(WikiFactoryService);
  //   expect(service).toBeTruthy();
  // });
});
