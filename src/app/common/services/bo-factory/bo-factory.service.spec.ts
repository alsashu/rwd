import { TestBed } from "@angular/core/testing";

import { BoFactoryService } from "./bo-factory.service";

describe("BoFactoryService", () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  // it('should be created', () => {
  //   const service: BoFactoryService = TestBed.get(BoFactoryService);
  //   expect(service).toBeTruthy();
  // });
});
